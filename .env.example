# === Template — copy to .env and fill in ===
# .env is gitignored — never commit it. See .claude/rules/secrets-guardrails.md

# GitHub (for gh CLI / github plugin, optional)
GITHUB_TOKEN=

# Anthropic / Claude API (in case skills/scripts are triggered via API, optional)
ANTHROPIC_API_KEY=

# Add project-specific variables here (with placeholder + comment)
# EXAMPLE_SERVICE_URL=
# EXAMPLE_API_KEY=
